{% extends 'layout.html.twig' %}

{% block title %}

{% endblock %}

{% block main %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-success">Valider</button>
    {{ form_end(form) }}
{% endblock %}

