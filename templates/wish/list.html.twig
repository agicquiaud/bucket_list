{% extends 'layout.html.twig' %}

{% block main %}

    <h2>Les idées !!</h2>

    <form class="form-group">
        <div>
            <input class="form-control" type="search" name="kw" placeholder="Lorem, ipsum..." value="">
        </div>
        <div>
            <button class="btn">OK!</button>
        </div>
    </form>

    <section>
        {% for wish in wishes %}
            <article>
                <h3>{{ wish.label }}</h3>
            </article>
        {% endfor %}
    </section>

{% endblock %}

{% block title %}Toutes les idées{% endblock %}